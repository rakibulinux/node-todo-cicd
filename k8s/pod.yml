apiVersion: v1
kind: Pod
metadata:
  name: node-pod
  namespace: node-app

spec:
  containers:
    - name: node-container
      image: rakibulinux/node-app-img
      ports:
        - containerPort: 8000
